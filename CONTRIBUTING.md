## Design Principles

Ciphermind follows these core principles:

- Deterministic by default
- Explainable and auditable outputs
- Registry-driven rules (no hardcoded crypto logic)
- AI is optional, advisory, and clearly labeled
- Security decisions must never depend on AI output
